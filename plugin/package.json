{
  "name": "@opencode-ai/agent-platform-plugin",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build index.ts --outdir dist --target node --format esm --external zod"
  },
  "dependencies": {
    "@opencode-ai/core": "workspace:*",
    "@opencode-ai/reflection": "workspace:*",
    "@opencode-ai/watchdog": "workspace:*",
    "@opencode-ai/plugin": "^1.4.0"
  }
}
