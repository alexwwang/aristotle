# 技术方案 M1: MCP 编排核心

**日期:** 2026-04-22
**前置文档:** GEAR 全工作流编排产品方案_260422.md (v1.2)
**范围:** `aristotle_mcp/server.py` + `aristotle_mcp/config.py`
**不涉及:** SKILL.md 重写、REFLECT.md/REVIEW.md/LEARN.md 删除、测试代码（M2 模块）

---

## 一、模块概述

M1 将 reflect / review / sessions 三个工作流纳入 MCP 编排，与已有 learn 流共享 `orchestrate_start` + `orchestrate_on_event` 模式。变更严格限定在 server.py 的编排区段（L989 之后追加）和 config.py 常量扩展。

### 变更统计

| 文件 | 行数 | 性质 |
|------|------|------|
| `server.py` | +~370 行 | 追加编排逻辑（L1227 后） |
| `config.py` | +5 行 | 新增 `SKILL_DIR` 常量 |

### 新增 MCP 工具

| 工具 | 用途 |
|------|------|
| `orchestrate_review_action` | Review 用户反馈处理 (confirm/revise/reject/re_reflect) |

### 扩展现有工具

| 工具 | 新增分支 |
|------|---------|
| `orchestrate_start` | +3 个 command: `reflect` / `review` / `sessions` |
| `orchestrate_on_event` | +3 个 event+phase 分支 |

---

## 二、文件变更清单

### 2.1 config.py

```python
# 新增 (L74 后)
SKILL_DIR = Path(__file__).parent.parent  # ~/.claude/skills/aristotle
```

**说明：** `SKILL_DIR` 指向 skill 根目录，prompt 模板通过 `{skill_dir}/REFLECTOR.md` 引用协议文件。支持 `ARISTOTLE_SKILL_DIR` 环境变量覆盖。

### 2.2 server.py

追加位置：L1227（现有编排代码末尾、`_rejected_dir_for` 之前）。

#### 变更分块

| 块 | 内容 | 估计行数 |
|----|------|---------|
| 2.2.1 | Prompt 模板常量 | ~40 |
| 2.2.2 | 辅助函数 | ~80 |
| 2.2.3 | `orchestrate_start` 扩展 | ~100 |
| 2.2.4 | `orchestrate_on_event` 扩展 | ~90 |
| 2.2.5 | `orchestrate_review_action` 新增 | ~70 |
| 2.2.6 | Auto-init / Cleanup | ~25 |
| **合计** | | **~405** |

---

## 三、server.py 变更详情

### 3.1 Prompt 模板常量

追加在编排区段开头（L989 区域附近，现有 `O_INTENT_PROMPT` 之后）：

```python
# ── Reflect flow prompt templates ──────────────────────────

REFLECTOR_PROMPT_TEMPLATE = """You are Aristotle's Reflector subagent. Read and execute the full protocol at
{skill_dir}/REFLECTOR.md (read the file first, then follow it step by step).

TARGET_SESSION_ID: {target_session_id}
PROJECT_DIRECTORY: {project_directory}
USER_LANGUAGE: {user_language}
FOCUS_HINT: {focus_hint}
DRAFT_SEQUENCE: {sequence}

Your output is NOT shown to the user. The Coordinator reads your session and \
extracts the DRAFT. Follow REFLECTOR.md exactly — especially STEP R5 to persist \
the DRAFT via persist_draft(sequence={sequence}, content=...).
"""

CHECKER_PROMPT_TEMPLATE = """You are Aristotle's Checker subagent. Read and execute the full protocol at
{skill_dir}/CHECKER.md (read the file first, then follow it step by step).

DRAFT_SEQUENCE: {sequence}
DRAFT_FILE: {draft_file}
PROJECT_DIRECTORY: {project_directory}

Your output is NOT shown to the user. Follow CHECKER.md exactly — read the \
DRAFT from DRAFT_FILE, validate each Reflection, and write rules via \
aristotle_write_rule / aristotle_stage_rule / aristotle_commit_rule.
"""

REVISE_PROMPT_TEMPLATE = """You are revising an Aristotle rule based on user feedback.

ORIGINAL RULE FILE: {rule_path}
ORIGINAL RULE CONTENT:
```
{original_content}
```

USER FEEDBACK: {feedback}

DRAFT CONTEXT:
```
{draft_summary}
```

Write the revised rule. Your output MUST follow this EXACT format:
Line 1: FILE: {rule_path}
Line 2+: The complete revised rule in YAML frontmatter + Markdown body format.
Do NOT include any other text, explanation, or commentary.
"""
```

### 3.2 辅助函数

#### `_next_sequence() -> int`

从 `aristotle-state.json` 读取当前记录数，返回 `len(records) + 1`。

```python
def _next_sequence() -> int:
    """计算下一个 sequence 编号。"""
    state_path = resolve_repo_dir().parent / "aristotle-state.json"
    if not state_path.exists():
        return 1
    try:
        records = json.loads(state_path.read_text(encoding="utf-8"))
        return len(records) + 1 if isinstance(records, list) else 1
    except (json.JSONDecodeError, ValueError):
        return 1
```

#### `_ensure_repo_initialized() -> None`

首次使用时自动初始化 repo。

```python
def _ensure_repo_initialized() -> None:
    """确保 repo 已初始化，首次使用时自动 init。"""
    repo_dir = resolve_repo_dir()
    if not (repo_dir / ".git").exists():
        init_repo()
```

#### `_build_reflector_prompt(...) -> str`

```python
def _build_reflector_prompt(
    target_session_id: str,
    focus_hint: str,
    sequence: int,
    project_directory: str = "",
    user_language: str = "en-US",
) -> str:
    # [M3 §7.1] focus_hint 截断至 200 字符防注入
    safe_focus = focus_hint[:200]
    return REFLECTOR_PROMPT_TEMPLATE.format(
        skill_dir=str(SKILL_DIR),
        target_session_id=target_session_id,
        project_directory=project_directory,
        user_language=user_language,
        focus_hint=safe_focus,
        sequence=sequence,
    )
```

#### `_build_checker_prompt(...) -> str`

```python
def _build_checker_prompt(
    sequence: int,
    draft_file: str,
    project_directory: str = "",
) -> str:
    return CHECKER_PROMPT_TEMPLATE.format(
        skill_dir=str(SKILL_DIR),
        sequence=sequence,
        draft_file=draft_file,
        project_directory=project_directory,
    )
```

#### `_parse_checker_result(result: str) -> tuple[int, int]`

从 C 输出中提取 committed / staged 计数。

```python
def _parse_checker_result(result: str) -> tuple[int, int]:
    """解析 C 输出，返回 (committed, staged) 计数。"""
    committed, staged = 0, 0
    # 匹配 "Committed: 3" 或 "Staged: 1" 等格式
    for line in result.split("\n"):
        m = re.match(r"(?:committed|Committed)\s*[:=]\s*(\d+)", line, re.IGNORECASE)
        if m:
            committed = int(m.group(1))
        m = re.match(r"(?:staged|Staged)\s*[:=]\s*(\d+)", line, re.IGNORECASE)
        if m:
            staged = int(m.group(1))
    # Fallback: 如果未找到显式计数，尝试从 "X rules committed, Y staged" 提取
    if committed == 0 and staged == 0:
        m = re.search(r"(\d+)\s+rules?\s+committed.*?(\d+)\s+staged?", result, re.IGNORECASE)
        if m:
            committed, staged = int(m.group(1)), int(m.group(2))
    return committed, staged
```

#### `_format_review_output(...) -> str`

格式化 review 展示内容（~30 行）。

```python
def _format_review_output(
    sequence: int,
    target_record: dict,
    draft_content: str,
    rules_result: dict,
) -> str:
    """格式化 review 输出。"""
    status = target_record.get("status", "?")
    target = target_record.get("target_label", "?")
    launched = target_record.get("launched_at", "?")[:16]

    lines = [
        f"🦉 Review #{sequence} — [{target}] {status} — {launched}",
        "",
    ]

    # DRAFT 摘要（截取前 2000 字符）
    if draft_content:
        preview = draft_content[:2000]
        if len(draft_content) > 2000:
            preview += "\n... (truncated)"
        lines.append("## DRAFT Report")
        lines.append(preview)
        lines.append("")

    # 关联规则
    rules = rules_result.get("rules", [])
    count = rules_result.get("count", 0)
    if count > 0:
        lines.append(f"## Associated Rules ({count})")
        for i, r in enumerate(rules[:10], 1):
            meta = r.get("metadata", {})
            summary = meta.get("error_summary", r.get("path", "?"))
            cat = meta.get("category", "?")
            rstatus = meta.get("status", "?")
            lines.append(f"  {i}. [{cat}/{rstatus}] {summary}")
    else:
        lines.append("No associated rules found.")

    lines.append("")
    lines.append("Choose an action:")
    lines.append("  1. confirm — Accept all staging rules")
    lines.append("  2. reject — Reject this reflection")
    lines.append("  3. revise N — Revise rule #N (append feedback after colon)")
    lines.append("  4. re-reflect — Request deeper analysis")

    return "\n".join(lines)
```

#### `_parse_revised_rule(content: str) -> tuple[str | None, str | None]`

解析 O revise 输出中的修订规则。

```python
def _parse_revised_rule(content: str) -> tuple[str | None, str | None]:
    """从 O revise 输出解析 (rule_path, rule_content)。
    
    期望格式:
      Line 1: FILE: <path>
      Line 2+: YAML frontmatter + Markdown body
    """
    lines = content.strip().split("\n")
    if not lines:
        return None, None

    # 提取 FILE: 行
    rule_path = None
    content_start = 0
    for i, line in enumerate(lines):
        m = re.match(r"^FILE:\s*(.+)$", line.strip())
        if m:
            rule_path = m.group(1).strip()
            content_start = i + 1
            break

    if not rule_path:
        return None, None

    rule_content = "\n".join(lines[content_start:]).strip()
    if not rule_content:
        return None, None

    return rule_path, rule_content
```

#### `_build_revise_prompt(rule_path: str, original_content: str, feedback: str, draft_summary: str) -> str`

构建 O revise prompt，转义用户内容中的花括号。

```python
def _build_revise_prompt(rule_path: str, original_content: str,
                         feedback: str, draft_summary: str) -> str:
    """构建 revise O prompt。转义花括号防止 format() KeyError。"""
    _esc = lambda s: s.replace("{", "{{").replace("}", "}}")
    # [M3 §7.1] feedback 截断至 2000 字符防注入
    safe_feedback = feedback[:2000]
    return REVISE_PROMPT_TEMPLATE.format(
        rule_path=rule_path,
        original_content=_esc(original_content),
        feedback=_esc(safe_feedback),
        draft_summary=_esc(draft_summary),
    )
```

#### `_cleanup_stale_workflows(max_age_hours: int = 24) -> None`

清理已完成和卡死的 workflow 文件。

```python
def _cleanup_stale_workflows(max_age_hours: int = 24) -> None:
    """清理过期 workflow 文件。
    
    规则:
    - phase=="done" 且 age > max_age_hours → 删除
    - phase in ("reflecting","checking","review","intent_extraction","search") 且 age > 2*max_age_hours → 删除
    - phase=="init" 且 age > 2*max_age_hours → 删除（异常：init 不应持续存在）
    """
    from datetime import timedelta

    workflows_dir = resolve_repo_dir() / WORKFLOW_DIR_NAME
    if not workflows_dir.exists():
        return

    cutoff = datetime.now(timezone.utc) - timedelta(hours=max_age_hours)
    stale_cutoff = datetime.now(timezone.utc) - timedelta(hours=max_age_hours * 2)

    for wf_file in workflows_dir.glob("*.json"):
        try:
            wf = json.loads(wf_file.read_text(encoding="utf-8"))
            phase = wf.get("phase", "")
            updated = wf.get("updated_at", "")
            if not updated:
                continue
            updated_dt = datetime.fromisoformat(updated)

            if phase == "done" and updated_dt < cutoff:
                wf_file.unlink()
            elif phase in ("reflecting", "checking", "review",
                           "intent_extraction", "search", "init") and updated_dt < stale_cutoff:
                wf_file.unlink()
        except (json.JSONDecodeError, ValueError):
            pass
```

### 3.3 orchestrate_start 扩展

在现有 `if command == "learn"` 分支之后、`elif command == "reflect"` 占位之前，替换并扩展：

```python
    # ── 已有 learn 分支 (L1120-1159) 不变 ──────────────────

    elif command == "reflect":
        # [状态机] init → reflecting (fire_sub R)
        target_session_id = args.get("target_session_id")
        focus = args.get("focus", "last")
        user_language = args.get("user_language", "en-US")
        project_directory = args.get("project_directory", "")

        if not target_session_id:
            return {"action": "notify", "message": "🦉 Need target_session_id."}

        sequence = _next_sequence()

        r_prompt = _build_reflector_prompt(
            target_session_id=target_session_id,
            focus_hint=focus,
            sequence=sequence,
            project_directory=project_directory,
            user_language=user_language,
        )

        _save_workflow(workflow_id, {
            "phase": "reflecting",
            "command": "reflect",
            "target_session_id": target_session_id,
            "sequence": sequence,
            "pending_role": "R",
            "record_created": False,
            "target_label": args.get("target_label", "unknown"),
            "project_directory": project_directory,
            "focus": focus,
            "user_language": user_language,
        })

        return {
            "action": "fire_sub",
            "workflow_id": workflow_id,
            "sub_prompt": r_prompt,
            "sub_role": "R",
            "notify_message": f"🦉 Aristotle Reflector launched [{args.get('target_label', 'unknown')}].\n"
                             "   Checker will validate automatically when done.",
        }

    elif command == "review":
        # [状态机] 一次性 → review (notify)
        try:
            sequence = int(args.get("sequence", 0))
        except (ValueError, TypeError):
            return {"action": "notify", "message": "🦉 Invalid sequence number. Usage: /aristotle review N"}
        if not sequence:
            return {"action": "notify", "message": "🦉 Usage: /aristotle review N"}

        # 加载 target record
        state_path = resolve_repo_dir().parent / "aristotle-state.json"
        if not state_path.exists():
            return {"action": "notify",
                    "message": "🦉 No reflection records yet. Run /aristotle first."}

        try:
            records = json.loads(state_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, ValueError):
            return {"action": "notify", "message": "🦉 State file corrupted."}

        target_record = None
        for i, r in enumerate(records):
            if i + 1 == sequence:
                target_record = r
                break

        if not target_record:
            return {"action": "notify",
                    "message": f"🦉 Reflection #{sequence} not found. "
                               f"Run /aristotle sessions to list."}

        # 加载 DRAFT 文件
        draft_path = target_record.get("draft_file_path", "")
        draft_content = ""
        if draft_path:
            dp = Path(draft_path).expanduser()
            if dp.exists():
                draft_content = dp.read_text(encoding="utf-8")

        # 查找关联规则
        target_session = target_record.get("target_session_id", "")
        rules_result = list_rules(
            status_filter="all",
            keyword=target_session,
            limit=20,
        )

        # 存储已展示规则路径，供 revise action 按 index 查找
        displayed_rules = [r.get("path", "") for r in rules_result.get("rules", [])]

        message = _format_review_output(sequence, target_record, draft_content, rules_result)

        _save_workflow(workflow_id, {
            "phase": "review",
            "command": "review",
            "sequence": sequence,
            "target_record": target_record,
            "displayed_rules": displayed_rules,
            "target_session_id": target_session,
            "re_reflect_count": target_record.get("re_reflect_count", 0),  # [R4-FIX] 从 record 继承
        })

        return {
            "action": "notify",
            "workflow_id": workflow_id,
            "message": message,
        }

    elif command == "sessions":
        # [状态机] 无状态查询，不创建 workflow
        state_path = resolve_repo_dir().parent / "aristotle-state.json"
        if not state_path.exists():
            return {"action": "notify", "message": "🦉 No reflection records yet."}

        try:
            records = json.loads(state_path.read_text(encoding="utf-8"))
        except (json.JSONDecodeError, ValueError):
            return {"action": "notify", "message": "🦉 State file corrupted."}

        if not records:
            return {"action": "notify", "message": "🦉 No reflection records yet."}

        lines = ["🦉 Reflection Records:"]
        for i, r in enumerate(records):
            status = r.get("status", "?")
            target = r.get("target_label", "?")
            rules = r.get("rules_count", "?")
            launched = r.get("launched_at", "?")[:16]

            status_icon = {
                "auto_committed": "✅",
                "partial_commit": "📋",
                "processing": "⏳",
                "checker_failed": "❌",
            }.get(status, "?")

            lines.append(f"  #{i+1} {status_icon} [{target}] {rules} rules — {launched}")

        # sessions 不返回 workflow_id（无后续事件）
        return {
            "action": "notify",
            "message": "\n".join(lines),
        }

    else:
        return {
            "action": "notify",
            "workflow_id": workflow_id,
            "message": f"🦉 Unknown command: {command}",
        }
```

### 3.4 orchestrate_on_event 扩展

重构现有 `o_done` dispatch 为 phase 白名单模式，新增 3 个事件处理分支。

**关键：dispatch 顺序必须严格按以下顺序执行。**

```python
    # ═══ 1. Learn flow: o_done + intent_extraction (现有，不变) ═══
    if event_type == "o_done" and workflow.get("phase") == "intent_extraction":
        # ... 现有逻辑 L1197-1213 不变 ...
        return _do_search_and_notify(workflow_id)

    # ═══ 2. Review flow: o_done + review (NEW) ═══
    # [状态机] review → done (O revise 完成)
    if event_type == "o_done" and workflow.get("phase") == "review":
        revised_content = data.get("result", "")
        if isinstance(revised_content, dict):
            revised_content = json.dumps(revised_content)

        rule_path, rule_content = _parse_revised_rule(str(revised_content))

        action_msg = ""
        if rule_path and rule_content:
            # 写入修订规则（覆盖原文件）
            resolved, err = _safe_resolve(rule_path)
            if resolved and resolved.exists():
                # 读取原始 frontmatter 保留元数据
                original_fm = read_frontmatter_raw(resolved) or {}
                resolved.parent.mkdir(parents=True, exist_ok=True)
                resolved.write_text(rule_content, encoding="utf-8")

                # 合并保留原始 frontmatter 字段（created_at, source_session 等）
                if original_fm:
                    new_fm = read_frontmatter_raw(resolved) or {}
                    for key in ("created_at", "source_session", "scope", "project_path"):
                        if key in original_fm and key not in new_fm:
                            update_frontmatter_field(resolved, key, original_fm[key])

                # Stage + audit decision
                try:
                    stage_rule(file_path=rule_path)
                except Exception:
                    pass

                decision = get_audit_decision(file_path=rule_path)
                if decision.get("audit_level") == "auto":
                    try:
                        commit_rule(file_path=rule_path)
                        action_msg = f"✅ Rule revised and auto-committed: {rule_path}"
                    except Exception as e:
                        action_msg = f"⚠️ Rule revised but commit failed: {e}"
                else:
                    action_msg = f"📋 Rule revised and staged: {rule_path}"
            else:
                action_msg = f"⚠️ Rule file not found: {rule_path}"
        else:
            action_msg = "⚠️ Could not parse revised rule from output."

        workflow["phase"] = "done"
        _save_workflow(workflow_id, workflow)

        return {
            "action": "notify",
            "workflow_id": workflow_id,
            "message": action_msg,
        }

    # ═══ 3. o_done catch-all (现有，不变) ═══
    if event_type == "o_done":
        return {
            "action": "notify",
            "workflow_id": workflow_id,
            "message": f"🦉 Unexpected o_done in phase '{workflow.get('phase')}'.",
        }

    # ═══ 4. Reflect flow: subagent_done + reflecting (NEW) ═══
    # [状态机] reflecting → checking (fire_sub C)
    if event_type == "subagent_done" and workflow.get("phase") == "reflecting":
        reflector_session_id = data.get("session_id", "")

        sequence = workflow.get("sequence")
        target_session_id = workflow.get("target_session_id")

        # [CRITICAL] R 完成后才创建 record（此时 reflector_session_id 可用）
        if not workflow.get("record_created"):
            create_reflection_record(
                target_session_id=target_session_id,
                target_label=workflow.get("target_label", "unknown"),
                reflector_session_id=reflector_session_id,
            )
            workflow["record_created"] = True

            # [R4-FIX] 将 re_reflect_count 传播至 reflection record，
            # 使后续 review workflow 能继承计数，实现跨周期 max-3 限制。
            rr_count = workflow.get("re_reflect_count", 0)
            if rr_count > 0:
                state_path = resolve_repo_dir().parent / "aristotle-state.json"
                if state_path.exists():
                    records = json.loads(state_path.read_text(encoding="utf-8"))
                    if records:
                        records[-1]["re_reflect_count"] = rr_count
                        state_path.write_text(
                            json.dumps(records, ensure_ascii=False, indent=2),
                            encoding="utf-8",
                        )

        # 计算 draft_file 路径（在 repo 外部）
        draft_file = str(
            resolve_repo_dir().parent / "aristotle-drafts" / f"rec_{sequence}.md"
        )

        c_prompt = _build_checker_prompt(
            sequence=sequence,
            draft_file=draft_file,
            project_directory=workflow.get("project_directory", ""),
        )

        workflow["phase"] = "checking"
        workflow["pending_role"] = "C"
        workflow["reflector_session_id"] = reflector_session_id
        _save_workflow(workflow_id, workflow)

        return {
            "action": "fire_sub",
            "workflow_id": workflow_id,
            "sub_prompt": c_prompt,
            "sub_role": "C",
        }

    # ═══ 5. Reflect flow: subagent_done + checking (NEW) ═══
    # [状态机] checking → done (notify)
    if event_type == "subagent_done" and workflow.get("phase") == "checking":
        result = data.get("result", "")
        if isinstance(result, dict):
            result = json.dumps(result)

        committed, staged = _parse_checker_result(str(result))
        sequence = workflow.get("sequence")

        status = "auto_committed" if staged == 0 else "partial_commit"
        complete_reflection_record(
            sequence=sequence,
            status=status,
            rules_count=committed + staged,
        )

        workflow["phase"] = "done"
        _save_workflow(workflow_id, workflow)

        return {
            "action": "notify",
            "workflow_id": workflow_id,
            "message": f"🦉 Aristotle done. {committed} rules committed, {staged} staged.\n"
                       f"   Review: /aristotle review {sequence}",
        }

    # ═══ 6. Catch-all ═══
    return {
        "action": "notify",
        "workflow_id": workflow_id,
        "message": f"🦉 Unhandled event_type '{event_type}' in phase '{workflow.get('phase')}'.",
    }
```

### 3.5 orchestrate_review_action 新增

新增独立 MCP tool，处理 review 阶段的用户交互。

```python
@mcp.tool()
def orchestrate_review_action(
    workflow_id: str,
    action: str,
    feedback: str = "",
    data_json: str = "{}",
) -> dict:
    """处理 Review 流的用户反馈。

    Args:
        workflow_id: Workflow ID（从 orchestrate_start("review") 返回值获取）
        action: "confirm" | "reject" | "revise" | "re_reflect"
        feedback: 用户反馈文本（revise 时使用）
        data_json: 额外数据（如 {"rule_index": N}）

    Returns dict with action, workflow_id, and optional fields.
    """
    workflow = _load_workflow(workflow_id)
    if not workflow:
        return {"action": "notify", "message": "🦉 Workflow not found."}

    if workflow.get("phase") != "review":
        return {"action": "notify", "workflow_id": workflow_id,
                "message": f"🦉 Workflow not in review phase (current: {workflow.get('phase')})."}

    sequence = workflow.get("sequence")

    # ── confirm ──────────────────────────────────────
    if action == "confirm":
        # 查找关联的 staging 规则并 commit
        target_session = workflow.get("target_session_id", "")
        committed_rule_paths = workflow.get("committed_rule_paths", [])

        committed = 0
        failed = 0
        if committed_rule_paths:
            # Phase 2+: 优先使用记录的路径
            for rp in committed_rule_paths:
                try:
                    commit_rule(file_path=rp)
                    committed += 1
                except Exception:
                    failed += 1
        else:
            # Phase 1 fallback: keyword 搜索
            rules_result = list_rules(status_filter="all", keyword=target_session, limit=20)
            for r in rules_result.get("rules", []):
                meta = r.get("metadata", {})
                if meta.get("status") == "staging":
                    try:
                        commit_rule(file_path=r.get("path", ""))
                        committed += 1
                    except Exception:
                        failed += 1

        # 更新 state record
        complete_reflection_record(sequence=sequence, status="auto_committed",
                                   rules_count=committed or None)

        workflow["phase"] = "done"
        _save_workflow(workflow_id, workflow)

        return {
            "action": "notify",
            "workflow_id": workflow_id,
            "message": f"✅ Review confirmed. {committed} rules committed." + (f" ⚠️ {failed} failed." if failed else ""),
        }

    # ── reject ───────────────────────────────────────
    elif action == "reject":
        # Reject 关联的 staging 规则
        target_session = workflow.get("target_session_id", "")
        rules_result = list_rules(status_filter="all", keyword=target_session, limit=20)
        rejected = 0
        for r in rules_result.get("rules", []):
            meta = r.get("metadata", {})
            if meta.get("status") in ("staging", "pending"):
                try:
                    reject_rule(file_path=r.get("path", ""))
                    rejected += 1
                except Exception:
                    pass

        complete_reflection_record(sequence=sequence, status="rejected")

        workflow["phase"] = "done"
        _save_workflow(workflow_id, workflow)

        return {
            "action": "notify",
            "workflow_id": workflow_id,
            "message": f"❌ Reflection #{sequence} rejected. {rejected} rules removed.",
        }

    # ── revise ───────────────────────────────────────
    elif action == "revise":
        # 构建修订 prompt，fire O
        displayed_rules = workflow.get("displayed_rules", [])
        if not displayed_rules:
            return {"action": "notify", "workflow_id": workflow_id,
                    "message": "🦉 No rules available to revise."}

        rule_index = 0
        try:
            extra = json.loads(data_json)
            rule_index = int(extra.get("rule_index", 0))
        except (json.JSONDecodeError, TypeError, ValueError):
            pass

        if not rule_index or rule_index < 1 or rule_index > len(displayed_rules):
            return {"action": "notify", "workflow_id": workflow_id,
                    "message": f"🦉 Invalid rule index. Choose 1-{len(displayed_rules)}."}

        rule_path = displayed_rules[rule_index - 1]
        resolved, err = _safe_resolve(rule_path)
        if not resolved or not resolved.exists():
            return {"action": "notify", "workflow_id": workflow_id,
                    "message": f"🦉 Rule file not found: {rule_path}"}

        original_content = resolved.read_text(encoding="utf-8")

        # DRAFT 摘要
        target_record = workflow.get("target_record", {})
        draft_path = target_record.get("draft_file_path", "")
        draft_summary = ""
        if draft_path:
            dp = Path(draft_path).expanduser()
            if dp.exists():
                draft_summary = dp.read_text(encoding="utf-8")[:1500]

        o_prompt = _build_revise_prompt(rule_path, original_content, feedback, draft_summary)

        workflow["pending_role"] = "O"
        workflow["revise_rule_path"] = rule_path
        _save_workflow(workflow_id, workflow)

        return {
            "action": "fire_o",
            "workflow_id": workflow_id,
            "o_prompt": o_prompt,
        }

    # ── re_reflect ───────────────────────────────────
    elif action == "re_reflect":
        re_reflect_count = workflow.get("re_reflect_count", 0)
        if re_reflect_count >= 3:
            return {"action": "notify", "workflow_id": workflow_id,
                    "message": "🦉 Max re-reflect (3) reached. Use /aristotle to start fresh."}

        # 创建独立新 workflow（不覆盖当前 review）
        new_workflow_id = f"wf_{uuid.uuid4().hex[:16]}"
        new_sequence = _next_sequence()

        target_session_id = workflow.get("target_session_id", "")
        r_prompt = _build_reflector_prompt(
            target_session_id=target_session_id,
            focus_hint=workflow.get("focus", "full"),
            sequence=new_sequence,
            project_directory=workflow.get("project_directory", ""),
            user_language=workflow.get("user_language", "en-US"),
        )

        _save_workflow(new_workflow_id, {
            "phase": "reflecting",
            "command": "reflect",
            "target_session_id": target_session_id,
            "sequence": new_sequence,
            "pending_role": "R",
            "record_created": False,
            "target_label": workflow.get("target_label", "unknown"),
            "project_directory": workflow.get("project_directory", ""),
            "focus": workflow.get("focus", "full"),
            "user_language": workflow.get("user_language", "en-US"),
            "parent_review_sequence": sequence,
            "parent_workflow_id": workflow_id,
            "re_reflect_count": re_reflect_count + 1,
        })

        # 标记当前 review 为 done
        workflow["phase"] = "done"
        _save_workflow(workflow_id, workflow)

        return {
            "action": "fire_sub",
            "workflow_id": new_workflow_id,
            "sub_prompt": r_prompt,
            "sub_role": "R",
            "notify_message": f"🦉 Re-reflecting (#{re_reflect_count + 1}/3)...",
        }

    else:
        return {"action": "notify", "workflow_id": workflow_id,
                "message": f"🦉 Unknown review action: {action}"}
```

### 3.6 入口集成

在 `orchestrate_start` 函数体开头（现有 L1109 `try:` 之后、`workflow_id` 分配之前）插入：

```python
    # Auto-init + cleanup（每次 orchestrate_start 入口执行）
    _ensure_repo_initialized()
    _cleanup_stale_workflows()
```

---

## 四、config.py 变更

```python
# 现有 L56 保持不变:
# WORKFLOW_DIR_NAME = ".workflows"

# L74 后新增:
import os

SKILL_DIR = Path(os.environ.get(
    "ARISTOTLE_SKILL_DIR",
    str(Path(__file__).parent.parent),
))
```

**说明：** `SKILL_DIR` 默认指向 `~/.claude/skills/aristotle`（即项目根目录）。`REFLECTOR.md`、`CHECKER.md` 等协议文件通过 `SKILL_DIR / "REFLECTOR.md"` 引用。环境变量 `ARISTOTLE_SKILL_DIR` 允许自定义安装路径。

---

## 五、数据流图

### 5.1 Reflect 流

```
SKILL.md
  │ orchestrate_start("reflect", {target_session_id, ...})
  ▼
[MCP] phase: init → reflecting
  │ return {action: "fire_sub", sub_prompt, sub_role: "R"}
  ▼
SKILL.md → task(prompt=R_prompt) ─────► R subagent (REFLECTOR.md)
  │                                         │ persist_draft()
  │ notification ◄──────────────────────────┘
  │ orchestrate_on_event("subagent_done", {workflow_id, session_id})
  ▼
[MCP] phase: reflecting → checking
  │ create_reflection_record(reflector_session_id)
  │ return {action: "fire_sub", sub_prompt, sub_role: "C"}
  ▼
SKILL.md → task(prompt=C_prompt) ─────► C subagent (CHECKER.md)
  │                                         │ write_rule + stage_rule
  │ notification ◄──────────────────────────┘
  │ orchestrate_on_event("subagent_done", {workflow_id})
  ▼
[MCP] phase: checking → done
  │ complete_reflection_record(status, rules_count)
  │ return {action: "notify", message: "... committed, ... staged"}
  ▼
SKILL.md → 展示结果 → STOP
```

### 5.2 Review 流

```
SKILL.md
  │ orchestrate_start("review", {sequence: N})
  ▼
[MCP] 一次性: 加载 state record + DRAFT + rules → 格式化
  │ return {action: "notify", message: formatted_review}
  ▼
SKILL.md → 展示 review 内容 + 菜单
  │ 用户选择 confirm/revise/reject/re_reflect
  ▼
orchestrate_review_action(workflow_id, action, feedback)
  │
  ├─ confirm → commit staging rules → done
  ├─ reject  → reject rules → done
  ├─ revise  → fire_o (REVISE_PROMPT_TEMPLATE) → o_done → write+commit → done
  └─ re_reflect → fire_sub(R, new_workflow) → 新 reflect 流
```

### 5.3 Sessions 流

```
SKILL.md
  │ orchestrate_start("sessions", {})
  ▼
[MCP] 读 state file → 格式化 → return notify
  │
  ▼
SKILL.md → 展示 → STOP
```

---

## 六、实现顺序与依赖

### 阶段 A：基础设施（无外部依赖）

| 步骤 | 内容 | 依赖 |
|------|------|------|
| A1 | `config.py` 新增 `SKILL_DIR` | 无 |
| A2 | Prompt 模板常量 | A1 |
| A3 | `_next_sequence()` | 无 |
| A4 | `_ensure_repo_initialized()` | 无 |
| A5 | `_cleanup_stale_workflows()` | 无 |
| A6 | `_build_reflector_prompt()` | A2 |
| A7 | `_build_checker_prompt()` | A2 |
| A8 | `_parse_checker_result()` | 无 |
| A9 | `_format_review_output()` | 无 |
| A10 | `_parse_revised_rule()` | 无 |

### 阶段 B：orchestrate_start 扩展

| 步骤 | 内容 | 依赖 |
|------|------|------|
| B1 | `sessions` command | A3 |
| B2 | `review` command | A9, A3 |
| B3 | `reflect` command | A6, A3, A4, A5 |
| B4 | 入口集成（auto-init + cleanup） | A4, A5 |

### 阶段 C：orchestrate_on_event 扩展

| 步骤 | 内容 | 依赖 |
|------|------|------|
| C1 | 重构 o_done dispatch（白名单模式） | 无 |
| C2 | `subagent_done` + `reflecting` handler | B3, A7, A8 |
| C3 | `subagent_done` + `checking` handler | C2, A8 |
| C4 | `o_done` + `review` handler | B2, A10 |

### 阶段 D：orchestrate_review_action

| 步骤 | 内容 | 依赖 |
|------|------|------|
| D1 | `confirm` action | B2 |
| D2 | `reject` action | B2 |
| D3 | `revise` action | B2, A10 |
| D4 | `re_reflect` action | B3, A6 |

### 阶段 E：验证

| 步骤 | 内容 |
|------|------|
| E1 | 现有 166 pytest 全部通过（回归） |
| E2 | `lsp_diagnostics` 清洁 |
| E3 | 手动验证 reflect → R → C → done 完整流程 |

### 关键路径

```
A1 → A2 → A6,A7 → B3 → C2 → C3 → E3
              B2 → C4 → D1-D4
              B1 → E1
```

最长路径：A1→A2→A6→B3→C2→C3，约 6 步。

---

## 七、状态机速查

```
orchestrate_start("reflect")  →  phase: reflecting
  subagent_done(R)            →  phase: checking
  subagent_done(C)            →  phase: done

orchestrate_start("review")   →  phase: review
  confirm/reject              →  phase: done
  revise + o_done             →  phase: done
  re_reflect                  →  新 workflow: reflecting

orchestrate_start("sessions") →  无状态，不创建 workflow

orchestrate_start("learn")    →  phase: intent_extraction
  o_done                      →  phase: search → done
```
